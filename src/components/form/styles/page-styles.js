export const pageStyles = (theme) => ({
  paperContainer: {
    backgroundColor: "#bc7474",
    padding: "18px",
    "& .MuiFormControl-root": {
      width: "100%",
    },
    "& .MuiInputBase-root": {
      width: "100%",
    },
  },
});
